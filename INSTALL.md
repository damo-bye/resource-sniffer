# 安装指南

本指南将帮助您安装和配置资源嗅探下载器插件。

## 📋 系统要求

- **浏览器**：Chrome 88+、Edge 88+、Firefox 78+、Opera 60+、Brave、Vivaldi 4.0+、Samsung Internet
- **操作系统**：Windows、macOS、Linux、Android（Samsung Internet）
- **权限**：需要网络访问和下载权限

## 🚀 安装步骤

### 方法一：开发者模式安装（推荐）

#### Chrome 浏览器

1. **下载插件源码**
   - 下载整个 `resource-sniffer` 文件夹到本地
   - 或使用 Git 克隆：`git clone <repository-url>`

2. **打开扩展管理页面**
   - 在地址栏输入 `chrome://extensions/`

3. **开启开发者模式**
   - 在页面右上角找到"开发者模式"开关
   - 打开开关

4. **加载插件**
   - 点击"加载已解压的扩展程序"按钮
   - 选择下载的 `resource-sniffer` 文件夹
   - 点击"选择文件夹"

5. **验证安装**
   - 插件图标应该出现在浏览器工具栏中
   - 点击图标应该能打开弹窗界面

#### Edge 浏览器

1. **下载插件源码**
   - 下载整个 `resource-sniffer` 文件夹到本地

2. **打开扩展管理页面**
   - 在地址栏输入 `edge://extensions/`

3. **开启开发者模式**
   - 在页面左下角找到"开发人员模式"开关
   - 打开开关

4. **加载插件**
   - 点击"加载解压缩"按钮
   - 选择下载的 `resource-sniffer` 文件夹
   - 点击"选择文件夹"

5. **验证安装**
   - 插件图标应该出现在浏览器工具栏中
   - 点击图标应该能打开弹窗界面

#### Firefox 浏览器

1. **下载插件源码**
   - 下载整个 `resource-sniffer` 文件夹到本地

2. **打开调试页面**
   - 在地址栏输入 `about:debugging#/runtime/this-firefox`

3. **临时加载插件**
   - 点击"临时载入附加组件"按钮
   - 选择 `resource-sniffer` 文件夹中的 `manifest.json` 文件
   - 点击"打开"

4. **验证安装**
   - 插件图标应该出现在浏览器工具栏中
   - 点击图标应该能打开弹窗界面

5. **注意事项**
   - Firefox 使用 Manifest V2/V3，某些 API 可能与 Chrome 有差异
   - 建议使用 Firefox 78+ 版本

#### Opera 浏览器

1. **下载插件源码**
   - 下载整个 `resource-sniffer` 文件夹到本地

2. **打开扩展管理页面**
   - 在地址栏输入 `opera://extensions/`

3. **开启开发者模式**
   - 在页面右上角找到"开发者模式"开关
   - 打开开关

4. **加载插件**
   - 点击"加载已解压的扩展程序"按钮
   - 选择下载的 `resource-sniffer` 文件夹
   - 点击"选择文件夹"

5. **验证安装**
   - 插件图标应该出现在浏览器工具栏中
   - 点击图标应该能打开弹窗界面

6. **注意事项**
   - Opera 完全兼容 Chrome 扩展
   - 建议使用 Opera 60+ 版本

#### Brave 浏览器

1. **下载插件源码**
   - 下载整个 `resource-sniffer` 文件夹到本地

2. **打开扩展管理页面**
   - 在地址栏输入 `brave://extensions/`

3. **开启开发者模式**
   - 在页面右上角找到"开发者模式"开关
   - 打开开关

4. **加载插件**
   - 点击"加载已解压的扩展程序"按钮
   - 选择下载的 `resource-sniffer` 文件夹
   - 点击"选择文件夹"

5. **验证安装**
   - 插件图标应该出现在浏览器工具栏中
   - 点击图标应该能打开弹窗界面

6. **注意事项**
   - Brave 完全兼容 Chrome 扩展
   - Shields 可能阻止某些功能，建议调整 Shields 设置
   - 某些网站可能需要添加到白名单

#### Vivaldi 浏览器

1. **下载插件源码**
   - 下载整个 `resource-sniffer` 文件夹到本地

2. **打开扩展管理页面**
   - 在地址栏输入 `vivaldi://extensions/`

3. **开启开发者模式**
   - 在页面右上角找到"开发者模式"开关
   - 打开开关

4. **加载插件**
   - 点击"加载已解压的扩展程序"按钮
   - 选择下载的 `resource-sniffer` 文件夹
   - 点击"选择文件夹"

5. **验证安装**
   - 插件图标应该出现在浏览器工具栏中
   - 点击图标应该能打开弹窗界面

6. **注意事项**
   - Vivaldi 完全兼容 Chrome 扩展
   - 高度可定制，某些设置可能影响插件功能
   - 建议使用 Vivaldi 4.0+ 版本

#### Samsung Internet

1. **下载插件源码**
   - 下载整个 `resource-sniffer` 文件夹到本地

2. **打开扩展管理页面**
   - 打开 Samsung Internet 浏览器
   - 进入设置 > 扩展

3. **加载插件**
   - 点击"加载已解压的扩展程序"按钮
   - 选择下载的 `resource-sniffer` 文件夹

4. **验证安装**
   - 插件图标应该出现在浏览器工具栏中
   - 点击图标应该能打开弹窗界面

5. **注意事项**
   - Samsung Internet 基于 Chromium，部分兼容 Chrome 扩展
   - 某些功能可能受限
   - 建议使用最新版本

### 方法二：打包安装

1. **打包插件**
   - 在扩展管理页面点击"打包扩展程序"
   - 选择 `resource-sniffer` 文件夹
   - 生成 `.crx` 文件（Chrome/Edge/Opera/Brave/Vivaldi）或 `.xpi` 文件（Firefox）

2. **安装打包文件**
   - Chrome/Edge/Opera/Brave/Vivaldi：将 `.crx` 文件拖放到扩展管理页面
   - Firefox：将 `.xpi` 文件拖放到浏览器窗口

## ⚙️ 初始配置

### 生成图标

1. 打开 `resource-sniffer/icons/generate-icons.html` 文件
2. 点击"生成图标"按钮预览图标
3. 点击"下载图标"按钮下载所有尺寸的图标
4. 将下载的图标文件放入 `resource-sniffer/icons` 目录
5. 重新加载插件

### 配置设置

1. 点击插件图标打开弹窗
2. 点击设置按钮（⚙️）进入设置页面
3. 根据需要调整以下设置：

   **嗅探设置**
   - 选择要检测的资源类型
   - 设置文件大小过滤规则

   **下载设置**
   - 设置下载保存路径
   - 配置并发下载数

   **界面设置**
   - 选择主题（浅色/深色）
   - 选择语言

4. 点击"保存设置"按钮

## 🧪 测试插件

### 测试资源检测

1. 打开一个包含多媒体内容的网页，例如：
   - 视频网站（YouTube、Bilibili 等）
   - 图片网站（Flickr、Unsplash 等）
   - 新闻网站（包含图片和视频）

2. 点击插件图标查看检测到的资源
3. 应该能看到视频、音频、图片等资源列表

### 测试下载功能

1. 在资源列表中选择一个资源
2. 点击"下载选中"按钮
3. 检查下载是否成功开始
4. 验证文件是否正确下载

### 测试预览功能

1. 选择一个视频或图片资源
2. 点击"预览"按钮
3. 验证预览窗口是否正确显示
4. 测试播放/查看功能

## 🔧 故障排除

### 插件无法加载

**问题**：点击"加载已解压的扩展程序"后没有反应

**解决方案**：
1. 确保选择了正确的文件夹（包含 `manifest.json` 的文件夹）
2. 检查浏览器控制台是否有错误信息
3. 确保所有文件都已正确创建

### 资源检测不工作

**问题**：浏览网页时没有检测到任何资源

**解决方案**：
1. 检查插件是否已启用
2. 确认网页包含可下载的资源
3. 检查设置中是否启用了对应的资源类型
4. 尝试刷新页面

### 下载失败

**问题**：点击下载后没有反应或下载失败

**解决方案**：
1. 检查浏览器下载设置
2. 确认有足够的磁盘空间
3. 检查网络连接
4. 查看浏览器控制台的错误信息

### 预览不工作

**问题**：点击预览后窗口空白或显示错误

**解决方案**：
1. 确认资源 URL 可访问
2. 检查浏览器是否支持该媒体格式
3. 尝试刷新页面后重新检测

## 📞 获取帮助

如果以上方法都无法解决问题，请：

1. 查看浏览器控制台的错误信息
2. 记录问题发生的步骤
3. 通过以下方式反馈：
   - GitHub Issues
   - 邮件联系开发者
   - 插件设置页面的"反馈"按钮

## 🔄 更新插件

### 开发者模式更新

1. 下载最新版本的插件源码
2. 替换本地的 `resource-sniffer` 文件夹
3. 在扩展管理页面点击刷新按钮（🔄）
4. 或者关闭再重新开启插件

### 自动更新

如果通过应用商店安装，插件会自动更新到最新版本。

## 🗑️ 卸载插件

1. 打开扩展管理页面
2. 找到"资源嗅探下载器"
3. 点击"移除"按钮
4. 确认卸载

---

**提示**：安装完成后，建议先在测试页面上验证插件功能是否正常工作。
