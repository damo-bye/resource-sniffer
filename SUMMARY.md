# 🎉 资源嗅探下载器 - 开发完成

恭喜！资源嗅探下载器插件已成功开发完成。本插件具备完整的资源嗅探、预览和下载功能，支持 Chrome、Edge 和 Firefox 浏览器。

## 📦 项目概览

### 核心功能

✅ **智能资源嗅探**
- 自动检测网页中的视频、音频、图片、文档、压缩包等资源
- 支持静态和动态加载的资源
- 实时更新资源列表

✅ **资源预览**
- 视频在线播放预览
- 音频在线播放预览
- 图片缩放查看
- 文件详细信息展示

✅ **下载管理**
- 单个资源一键下载
- 多个资源批量下载
- 下载进度实时显示
- 下载历史记录

✅ **个性化设置**
- 自定义检测的资源类型
- 设置文件大小过滤规则
- 配置下载参数
- 界面主题和语言设置

### 技术特点

- **Manifest V3**：采用最新的浏览器扩展规范
- **原生 JavaScript**：无框架依赖，轻量高效
- **模块化设计**：代码结构清晰，易于维护和扩展
- **跨浏览器兼容**：支持 Chrome、Edge、Firefox

## 📁 文件结构

```
resource-sniffer/
├── manifest.json          # 插件配置文件
├── background.js          # 后台脚本（网络监听）
├── content.js             # 内容脚本（页面资源提取）
├── popup/
│   ├── popup.html         # 弹窗界面
│   ├── popup.css          # 弹窗样式
│   └── popup.js           # 弹窗逻辑
├── options/
│   ├── options.html       # 设置页面
│   ├── options.css        # 设置样式
│   └── options.js         # 设置逻辑
├── lib/
│   ├── sniffer.js         # 资源嗅探模块
│   ├── downloader.js      # 下载管理模块
│   └── storage.js         # 存储工具模块
├── icons/
│   ├── icon.svg           # 矢量图标
│   └── generate-icons.html # 图标生成工具
├── _locales/
│   ├── zh_CN/
│   │   └── messages.json  # 中文语言包
│   └── en/
│       └── messages.json  # 英文语言包
├── README.md              # 使用说明
├── INSTALL.md             # 安装指南
├── test.html              # 测试页面
└── SUMMARY.md             # 项目总结（本文件）
```

## 🚀 快速开始

### 1. 生成图标

```bash
# 在浏览器中打开图标生成工具
open resource-sniffer/icons/generate-icons.html

# 点击"下载图标"按钮
# 将下载的图标文件放入 icons 目录
```

### 2. 安装插件

**Chrome / Edge:**
1. 打开 `chrome://extensions/` 或 `edge://extensions/`
2. 开启"开发者模式"
3. 点击"加载已解压的扩展程序"
4. 选择 `resource-sniffer` 文件夹

**Firefox:**
1. 打开 `about:debugging#/runtime/this-firefox`
2. 点击"临时载入附加组件"
3. 选择 `manifest.json` 文件

### 3. 测试插件

```bash
# 在浏览器中打开测试页面
open resource-sniffer/test.html

# 按照页面说明进行测试
```

## 📖 使用指南

### 基本使用

1. **浏览网页**：正常浏览网页，插件会自动检测资源
2. **查看资源**：点击浏览器工具栏中的插件图标
3. **选择资源**：在弹出窗口中选择需要下载的资源
4. **下载资源**：点击"下载选中"或"下载全部"按钮

### 高级功能

- **资源筛选**：使用类型按钮和搜索框筛选资源
- **批量下载**：使用复选框选择多个资源批量下载
- **资源预览**：点击预览按钮查看资源详情
- **个性化设置**：点击设置按钮自定义插件行为

## 🎯 核心模块说明

### 1. 资源嗅探模块 (sniffer.js)

负责识别和分类网页中的各类资源，支持：
- 视频类型：MP4、WebM、AVI、MOV 等
- 音频类型：MP3、WAV、OGG、AAC 等
- 图片类型：JPG、PNG、GIF、WebP、SVG 等
- 文档类型：PDF、DOC、XLS、PPT 等
- 压缩包类型：ZIP、RAR、7Z、TAR 等

### 2. 下载管理模块 (downloader.js)

负责处理资源的下载逻辑，支持：
- 单个资源下载
- 批量下载（可配置并发数）
- 下载进度跟踪
- 下载状态管理
- 下载历史记录

### 3. 存储工具模块 (storage.js)

负责管理插件的设置和数据存储，支持：
- 设置的保存和加载
- 下载历史的管理
- 自定义过滤规则
- 数据导入导出

### 4. 后台脚本 (background.js)

负责后台逻辑处理，包括：
- 监听网络请求
- 管理资源嗅探
- 处理消息通信
- 管理下载任务
- 更新插件图标

### 5. 内容脚本 (content.js)

负责在网页中提取资源信息，包括：
- 提取视频、音频、图片资源
- 监听动态加载的资源
- 与后台脚本通信

## 🔧 自定义和扩展

### 添加新的资源类型

在 `lib/sniffer.js` 中添加新的 MIME 类型和文件扩展名映射：

```javascript
// 在 MIME_TYPE_MAP 中添加
'application/x-new-type': ResourceType.NEW_TYPE,

// 在 EXTENSION_MAP 中添加
'.newext': ResourceType.NEW_TYPE
```

### 修改界面样式

编辑 `popup/popup.css` 和 `options/options.css` 文件来自定义界面外观。

### 添加新功能

1. 在相应的模块中添加新方法
2. 更新消息处理逻辑
3. 更新界面交互代码

## 🐛 调试技巧

### 查看日志

1. 打开浏览器开发者工具
2. 查看 Console 面板的日志输出
3. 后台脚本的日志在扩展管理页面的"检查视图"中查看

### 常见问题

**Q: 插件无法加载？**
A: 确保选择了正确的文件夹（包含 manifest.json 的文件夹）

**Q: 资源检测不工作？**
A: 检查插件是否已启用，确认网页包含可下载的资源

**Q: 下载失败？**
A: 检查浏览器下载设置，确认有足够的磁盘空间

## 📈 性能优化建议

1. **合理设置过滤规则**：忽略不需要的资源类型和域名
2. **调整并发下载数**：根据网络状况调整最大并发下载数
3. **定期清理历史**：定期清除下载历史以释放存储空间
4. **使用资源预览**：下载前先预览，避免下载错误的资源

## 🔮 未来计划

- [ ] 支持更多资源类型
- [ ] 添加下载速度限制
- [ ] 支持断点续传
- [ ] 添加资源搜索功能
- [ ] 支持云端同步设置
- [ ] 添加快捷键支持
- [ ] 支持更多语言

## 📞 反馈和支持

如有问题或建议，请通过以下方式联系：

- **GitHub Issues**：提交问题和建议
- **邮件联系**：发送邮件至开发者
- **插件反馈**：使用设置页面的"反馈"按钮

## 📄 许可证

本项目采用 MIT 许可证，详情请查看 LICENSE 文件。

---

**感谢使用资源嗅探下载器！** 🎉

希望这个插件能帮助您更方便地下载网页资源。如有任何问题或建议，欢迎随时反馈！
